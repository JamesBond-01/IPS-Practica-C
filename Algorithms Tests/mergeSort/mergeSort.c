/*
 * Merge-Sorting es un metodo para ordenar arrays de forma eficiente.
 *
 * El mismo esta basado en el algoritmo de Divide & Conquer, que trata de dividir un problema grande en varios
 * mas chicos, para facilitar la resolucion de este.
 *
 * El Merge-Sorting funciona de la siguiente manera:
 *      - Primero, se divide el array en pequeñas listas lo mas reducidas posibles. Es decir, listas de 1 elemento.
 *      - Luego, se compara cada elemento con su lista adyacente y ambos elementos se unen en una nueva lista, de
 *      forma ordenada, compuesta por los mismos. Esta comapracion y union de listas se repite hasta obtener
 *      un unico array ordenado.
 */

#include <stdio.h>

int main() {


    return 0;
}